# V0.5 实施报告 — 全球学校搜索与筛选数据底座

> 完成日期：2026-09-22
> 分支：`global-study-mvp`
> 线上预览：https://global-study-mobile-mvp.onrender.com

## 一、做了什么（Task 1 → Task 8）

### Task 1：整理项目结构 ✅
- 数据按规格拆分为 `data/reference/`（locations、curricula、languages、education-stages）与 `data/demo/`（schools、programs、scholarships）。
- 后端保持单文件 `server.js` + Express，未引入新框架；`render.yaml`、`npm install` + `npm start` 部署链路不变。
- 删除与产品无关的遗留文件 `mvp-server.js`、`mvp-server-v2.js`、`data/reference.json`、`data/schools.demo.json`（这些文件未被 `npm start` / `render.yaml` 引用，删除不影响线上）。

### Task 2：建立参考字典 ✅
- `data/reference/locations.json`：2 个国家（新加坡 SG / 中国 CN）、3 个城市（SG-SIN、CN-SHA、CN-BJS）、上海 16 区 + 北京 16 区 + 新加坡 5 区域 45 规划区；地铁/轻轨线路 28 条、站点/机场/火车站 118 个（部分种子数据，用于验证结构，后续可继续导入）。
- `data/reference/education-stages.json`：10 个教育阶段（preschool → PhD），附 zh/en。
- `data/reference/curricula.json`：课程族 + 50+ 课程条目（IB / Cambridge / Edexcel / 美式 / 英式 / 加 / 澳 / 新 / 蒙台梭利等），附 aliases。
- `data/reference/languages.json`：15 种教学语言 + 8 类学校类型字典（international-school / bilingual-school / private-school / public-school / university / college / language-school / vocational-institution）。
- 所有字典 `source_note` 标注为「分类法 / 字典，非学校数据，不构成任何合作或招生授权」。

### Task 3：学校数据模型 ✅
- `data/demo/schools.json`：20 条演示学校，覆盖 SG-SIN / CN-SHA / CN-BJS。
- 实体字段对齐 `V0.5_SPEC.md`：Institution / Campus / Program / Scholarship 全部以 snake_case 建模。
- 每条明确 `is_demo: true`、`verified_status: "demo"`、`source_url: null`。
- `data/demo/programs.json`：40 条演示 Program，关联 institution_id / stage / curriculum / tuition / deadline / language_requirement。
- `data/demo/scholarships.json`：演示 Scholarship，关联 institution_id / program_id，带 certainty 标记。
- **未**把任何 demo 学校写成真实学校；**未**用 AI 猜测/补全学校事实。

### Task 4：搜索 API ✅
- `GET /api/reference`：返回 countries / cities / districts / educationStages / curriculumFamilies / curricula / languages / institutionTypes / transitLines / transitStations。
- `GET /api/schools`：支持 q、country、city、district、stage、curriculum、language、institutionType、minFee、maxFee、scholarship、boarding、transitStation、transitNear、verifiedOnly、sort、page、pageSize；参数可组合；返回 `meta.total/page/pageSize/demoOnly`；非法参数不崩溃（未知阶段返回空 200）。
- `GET /api/schools/:id`：聚合 institution + campuses + programs + scholarships + 来源溯源字段，未命中 404 JSON。
- `GET /api/programs`：支持 institution / stage / curriculum / language / minFee / maxFee / q 筛选，复用同一分页器。
- 保留并兼容上一版本 `GET /api/bootstrap` 与 C/B/S/Admin 四端业务流（applications / clients / audits / reset），未破坏原有入口。

### Task 5：手机 H5 搜索首页 ✅
- 学生端首页 hero CTA 调整为「开始找学校」→ 找学校页。
- 第一屏控件：国家、城市、教育阶段、课程体系、关键词 + 搜索按钮（全部由 `/api/reference` 动态生成）。
- 快捷筛选 chips：国际学校、IB、Cambridge、学费较低（按 fee_asc 排序）、有奖学金、有住宿、靠近公共交通（transitNear 结构位，距离算法留待 V1.0）。
- 角色切换（student / agency / supplier / admin）与 localStorage 记忆保留。

### Task 6：结果页 ✅
- 每张卡片显示：学校名称、国家/城市/区、阶段、课程、语言、住宿、奖学金、学费区间、最近交通、演示/已验证标识、更新时间、数据状态。
- 按钮：查看详情（`/api/schools/:id`）、对比（localStorage，最多 4 所）、收藏（localStorage）。
- 搜索结果由 `/api/schools` 驱动（不再前端内存过滤）。

### Task 7：高级筛选 ✅
- 手机 Bottom Sheet：`advancedSheet()`。
- 筛选项从 `/api/reference` 动态生成：区域（城市/行政区级联）、课程体系（按 family 分组）、教学语言、学费范围、学校类型、住宿、奖学金。
- 无前端写死的筛选项。

### Task 8：测试 ✅
- `tests/smoke.test.js`：23 个用例覆盖 health、reference、schools 基础/分页/组合/费用/布尔/语言/类型/交通/关键词/非法参数、schools/:id 详情与 404、programs 筛选、bootstrap 兼容、application 400、未知路由 JSON 404、应用启动 smoke。
- 结果：**23 passed, 0 failed**。

## 二、没做什么（明确边界）
- 未做 V1.0 的学校详情页「地图与距离/通勤」计算（V0.5 仅建立 transitStation / nearest_station_id 结构位与 transitNear 开关）。
- 未做真实学校数据导入工具、后台审核队列、source_url 录入后台。
- 未做复杂支付、贷款、放款/还款。
- 未引入微服务 / Kubernetes。
- 未修改 `main` 分支。
- 未把任何 demo 数据伪装为真实学校，未生成虚构排名/录取率/佣金协议。

## 三、哪些是 demo 数据
- `data/demo/*` 全部为演示数据，`is_demo: true`，`verified_status: "demo"`，`source_url: null`。
- `data/reference/*` 为分类字典（地区/阶段/课程/语言），来自产品资料，**不等于**任何真实学校合作、招生授权或商业关系。
- 前端详情页对 `is_demo` 学校显式标注「演示学校数据，仅用于验证搜索与详情结构」。

## 四、下一步 V1.0 做什么
1. 学校详情页地图与距离/通勤字段（基于 transitStation 经纬度）。
2. 最多 4 所对比（已可用，增强表格与差异高亮）。
3. 收藏 / 提醒（localStorage → 可服务端化）。
4. 真实数据导入工具（带 source_url / verified_at / effective dates 校验）。
5. 后台数据审核队列（verified_status 流转）。
6. B 端学校搜索视图、佣金字段展示（仅展示有真实协议或后台录入的数据）。

## 五、当前线上 URL 状态
- 部署分支 `global-study-mvp`，由 Render 自动部署。
- 本地 `npm test` 23/23 通过，`npm start` 可启动，`GET /api/health` 正常。
- 提交并推送后将触发 Render 重新部署；本报告生成前已在本地全量验证，未破坏 C/B/S/Admin 入口。
