# V1.0 CHANGELOG — 全球学校搜索正式版

> 发布日期：2026-09-23
> 分支：`workbuddy/v1.0-school-search` → 已合并 `global-study-mvp`（`main` 未动）
> 线上：https://global-study-mobile-mvp.onrender.com （Render 已部署并验证）

## Added（新增）
### C端搜索体验
- 学校详情页正式化：`#/school/:id` hash 路由独立页 + 结果卡详情 Bottom Sheet，覆盖基础资料/校区/阶段/课程/语言/学费与首年费用/奖学金/住宿/最近交通/图片/Program 列表/数据来源/verified_status/verified_at/更新时间；演示学校顶部醒目橙色横幅。
- 地图数据接口 `GET /api/map/:id`：结构化中心坐标 + 按距离排序临近站点（`maxStations`/`within` 参数）；`tile_provider` 恒为 null，不伪造地图。
- 距离字段：`distance_m`（haversine）+ `distance_label`（米/公里）；`commute_label` 恒为 null，前端显示「暂无可靠通勤数据」。
- 最多 4 所对比：`GET /api/compare` 服务端聚合 + 横向滚动对比表 + 差异项高亮（9 个维度）。
- 收藏与提醒：学校/课程收藏（localStorage 持久化）、申请截止/开学/奖学金截止三类站内提醒结构。
- 搜索排序新增 `sort=distance_asc`；142 个站点坐标全部补齐（`scripts/migrate-v1.js`）。

### 数据导入与审核
- 真实学校数据导入工具 `lib/importer.js`：CSV/JSON 双格式、字段别名归一化、字典匹配（城市在国家范围内解析）、字段校验、重复检测（城市+名称归一化）、逐行错误报告、批量导入。
- API：`POST /api/admin/import/preview`（预览校验）与 `POST /api/admin/import/commit`（批量导入，仅 admin）。
- 来源与有效期机制：`source_url` / `supplier_evidence` / `verified_status` / `verified_at` / `effective_from` / `effective_to`；**无来源的数据不得标记 verified**（强制降级 draft）。
- 数据审核后台：`GET /api/admin/queue`、`POST /api/admin/queue/:schoolId/action`（submit/approve/reject/request-info/expire）、`GET /api/admin/schools/:id/revisions`（历史版本 + 字段级修订）、`PATCH /api/admin/schools/:id`。
- 状态机：draft → pending → verified → expired / rejected；非法流转 400；**学费/奖学金等敏感字段变更强制回 pending 复审**。
- draft/pending/expired/rejected 学校不出现在 C 端搜索。
- 运行时存储 `lib/store.js` + `data/store/`（queue / imports / agreements）。
- 管理后台 H5「数据审核」页：审核队列操作 + 导入工具 UI。

### B端
- `GET /api/b2b/schools` 与 `/api/b2b/schools/:id`：学校 + 招生阶段 + 是否接受代理 + 合作状态 + 佣金类型/数值/结算周期字段（需 `x-role: agency|admin`）。
- `GET /api/b2b/programs`：B端 Program 视图，含协议层级（institution/program）与合作状态。
- **佣金红线**：佣金字段只可能来自 `is_demo=false` 的真实协议；演示协议显示「该记录为演示数据，无真实佣金信息」，无协议显示「暂无代理协议记录」；无任何自动生成/推断佣金的代码路径（有测试锁定）。
- H5 B端「学校资源」页新增「学校资源 / Program 列表」双 tab。

### 工程化
- GitHub Actions CI（`.github/workflows/ci.yml`）：npm install → npm test（Node 18/20 矩阵）→ 服务启动 + `/api/health` + H5 角色 smoke；CI 不通过禁止合并。
- `.gitattributes` 统一换行符，消除测试重写 JSON store 造成的假脏状态。
- 测试：新增 24 项 V1.0 测试并入 `npm test`（合计 47 项：V0.5 23 + V1.0 24）。
- 四份文档：`V1.0_IMPLEMENTATION_REPORT.md`、`V1.0_TEST_REPORT.md`、`REAL_DATA_IMPORT_GUIDE.md`、`V1.0_CHANGELOG.md`。

## Changed（变更）
- `npm test` 现依次运行 V0.5 smoke 与 V1.0 两套件。
- 导入记录自动补齐 `name_zh`/`name_en`（按语言分流），与演示数据同构，保证 C 端搜索可命中。
- 详情 Sheet 与独立详情页展示到站距离与站点线路代码。
- B端工作台导航文案：「学校库」→「学校资源」，并新增数据审核页到 admin 导航。

## Fixed（修复）
- `haversineMeters` 对 null/空坐标按 0 处理返回 157km 假距离 → 现返回 null。
- `parseInput` 对不可解析文本返回 `[]` 假装成功 → 现返回 null，预览接口 400。
- 导入报告重复行同时计入 invalid 与 duplicates，三个计数之和不等于 total → 重复单独计数。
- 「Singapore, Singapore」城市解析误命中国家字典得到错误 city_id → 城市先在国家范围内解析。
- 导入学校缺 name_zh/name_en 导致审核通过后 C 端搜索查不到 → 导入时按语言补齐。
- 地图区域线路代码渲染为未插值字面量 `${esc(s.line.code)}` → 正确显示 DT/CC/EW 等。
- 审核动作 note 文案错误（submit 显示「标记过期」）→「提交审核」。

## 未做（遵守边界）
- 会员收费 V1.2、完整支付、贷款、住宿交易系统、Kubernetes、微服务拆分——全部未做。
- 未虚构任何学校/排名/佣金/录取概率；未删除四端角色入口；未修改 main。
- 未导入任何真实学校数据（工具与审核链路已就绪，等待运营提供带来源数据）。

## 升级说明
- 部署方式不变：`npm install` + `npm start`，Render 从 `global-study-mvp` 自动部署。
- 新增运行时目录 `data/store/`（首次启动自动创建；仓库内为空种子文件）。
- CI 文件因当前 token 缺 `workflow` scope 暂存于本地分支 `ci/pending-workflow`，授权后：
  `git checkout ci/pending-workflow -- .github/workflows/ci.yml && git add .github && git commit -m "ci: restore workflow" && git push`
