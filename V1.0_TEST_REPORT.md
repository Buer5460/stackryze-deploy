# V1.0 测试报告

> 执行时间：2026-09-23
> 命令：`npm install` → `npm test`（一条命令依次运行 V0.5 smoke + V1.0 两套件）
> 运行环境：Node.js（engines >= 18），本机 Node 24；CI 矩阵为 Node 18.x / 20.x
> 结果总览：**47 passed, 0 failed**（V0.5 smoke 23 + V1.0 24）

测试为纯 Node + Express 进程内 `fetch` 调用，不依赖外部服务；测试结束自动清理导入数据，`data/store/*` 恢复空种子状态。

## 一、V0.5 smoke 套件（23 项，回归保障）

| # | 用例 | 结果 |
| --- | --- | --- |
| 1 | data integrity：20 条 demo school 全 is_demo + verified_status=demo + source_url=null | ok |
| 2 | reference：3 cities；上海 16 + 北京 16 + 新加坡 45 规划区 | ok |
| 3 | reference：stages 10、curricula 53、languages 15、transit 已种子 | ok |
| 4 | GET /api/health | ok |
| 5 | GET /api/reference 返回全部字典 | ok |
| 6-16 | /api/schools 基础/分页/组合/费用/布尔/语言/类型/交通站/关键词/非法参数不崩溃（11 项） | ok |
| 17-18 | /api/schools/:id 详情聚合 与 404 | ok |
| 19 | /api/programs 筛选 | ok |
| 20 | /api/bootstrap 保留 C/B/S/Admin 演示 payload | ok |
| 21 | POST /api/applications 非法 payload 400 | ok |
| 22 | 未知 /api 路径 JSON 404 | ok |
| 23 | app start smoke：GET / 含四端角色 | ok |

## 二、V1.0 套件（24 项）

### 距离与详情
| # | 用例 | 结果 |
| --- | --- | --- |
| 1 | distance：haversine 同点 ~0m；已知站对 ~3.5km；null 坐标返回 null（非 0） | ok |
| 2 | distance：20 所学校全部有到站距离；label 为米/公里；commute_label 恒为 null | ok |
| 3 | detail：详情含坐标、距离、首年成本、images/programs 数组、source 块 | ok |
| 4 | detail：任务书要求的 16 个字段全部存在（含 source 块 9 个溯源字段）；demo 记录 is_demo=true / verified_status=demo / source_url=null | ok |

### 地图与对比
| # | 用例 | 结果 |
| --- | --- | --- |
| 5 | map：/api/map/:id 返回结构化坐标 + 按距离排序站点（含线路与距离文案）；tile_provider=null 不伪造 | ok |
| 6 | map：maxStations/within 生效；未知学校 404；无坐标学校诚实返回 renderable=false | ok |
| 7 | compare：返回请求学校、超 4 所截断为 4 | ok |
| 8 | favourites：bootstrap schools 数组支撑收藏/对比 | ok |

### 导入与审核
| # | 用例 | 结果 |
| --- | --- | --- |
| 9 | import：parseInput 支持 JSON 数组与 CSV；垃圾输入返回 null | ok |
| 10 | import：无来源请求 verified → 报错并降级 draft；带 source_url+verified_at 通过并正确解析国家/城市 | ok |
| 11 | import：重复检测（city_id + 名称归一化） | ok |
| 12 | import preview API：混合行 total=3 → importable=1 / duplicates=1 / invalid=1，错误含「重复」「必填字段」 | ok |
| 13 | import commit：草稿入队；**C 端搜索不可见 draft** | ok |
| 14 | review：draft→pending→approve（有来源）→ verified 且 C 端可见；敏感字段编辑 → 强制回 pending 且 C 端再次不可见 | ok |
| 15 | review：reject 流转 + 历史记录（≥3 条）+ 字段级修订（tuition_min old→new） | ok |
| 16 | review：无来源 approve → 400「缺少来源」 | ok |
| 17 | cleanup：测试导入数据自动清理，store 恢复空 | ok |

### B端权限与佣金
| # | 用例 | 结果 |
| --- | --- | --- |
| 18 | B2B：agency 角色可见学校视图；student 403 | ok |
| 19 | B2B：佣金只可能来自真实协议——全库无一条真实佣金值；演示协议只给「演示」提示且 commission_type=null | ok |
| 20 | B2B programs：视图含 agency/commission/accepts_agents/cooperation_status/settlement_cycle；无真实协议时零佣金值（不虚构） | ok |
| 21 | B2B programs：institution / stage 筛选正确 | ok |
| 22 | B2B programs：无角色 403 / supplier 403 / admin 200 | ok |
| 23 | B2B：admin 可访问；supplier 403 | ok |
| 24 | admin import/queue 端点拒绝非 admin | ok |

## 三、浏览器交互验收（真实渲染，本地 + 线上）

| 交互 | 结果 |
| --- | --- |
| 首页（375×800） | hero CTA「开始找学校」、演示学校卡、演示徽标 |
| 找学校页 | 20 所 · 第 1 页；国家→城市级联；阶段/课程（按族分组）/关键词；9 个快捷 chip；筛选标签区；一键清除 |
| 结果卡详情 Sheet | 演示横幅、基础资料、学费、奖学金、到站距离 712 米、校区、课程（可收藏）、数据来源 |
| 独立详情页 #/school/:id | 最近 5 站点（第六大道 DT 712 米 / 植物园 CC 2.4 公里…）、「暂无可靠通勤数据」、首年总成本、图片区、数据来源与审核状态 |
| 对比页 #/compare | 3 校对比、9 个维度、24 个差异高亮格、横向滚动可用、上限 4 所 |
| B端学校资源 | 招生阶段/接受代理/合作状态/佣金字段（未录入）+「Program 列表」tab（40 个 Program，含协议层级与佣金提示） |
| 管理后台数据审核 | 审核队列渲染、导入 textarea、预览校验/批量导入按钮 |
| 四端角色 | student / agency / supplier / admin 导航与页面全部正常 |

## 四、手机三档宽度布局（程序化几何检查 + 真实浏览器）

| 宽度 | 横向溢出 | 结果卡宽 | 底部导航 |
| --- | --- | --- | --- |
| 375 | 0px | 332px | 4 × 90px 等宽 |
| 390 | 0px | 347px | 4 × 94px 等宽 |
| 430 | 0px | 387px | 4 × 104px 等宽 |

线上（390px 视口）复测：首页、搜索（20 所 · 第 1 页）、详情页地图区域与演示横幅均正常，横向溢出 0px。微信/Safari 直接访问 H5 可完整使用（无构建依赖的静态单文件 + API）。

## 五、CI smoke 验证
`.github/workflows/ci.yml` 的三步（npm install → npm test → 起服务后 curl /api/health 校验 `"status":"ok"` + H5 角色文案）已在本地逐条执行通过。workflow 文件推送受阻于 token 权限（详见实施报告「六、风险与待办」），授权后一条命令即可恢复门禁。

## 六、本轮修复（测试驱动 + 浏览器验收发现）
1. haversine 对 null 坐标返回 157km 假距离 → 返回 null（测试 1 锁定）。
2. parseInput 垃圾输入返回 [] → 返回 null（测试 9 锁定）。
3. 重复行重复计数导致 importable+duplicates+invalid ≠ total（测试 12 锁定）。
4. 城市解析误命中国家字典（测试 10 的 city_id 断言锁定）。
5. 导入学校缺 name_zh/name_en 导致审核通过后搜索查不到（测试 14 锁定）。
6. 地图线路代码渲染为未插值字面量（浏览器验收发现，线上复测通过）。

## 七、结论
47/47 通过；任务书要求的测试方向（详情/compare/favorites/import validation/duplicate detection/review status/source validation/distance calculation/B端权限）全部覆盖。V0.5 功能无回归。
