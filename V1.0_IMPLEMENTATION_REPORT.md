# V1.0 实施报告 — 全球学校搜索正式版

> 完成日期：2026-09-23
> 开发分支：`workbuddy/v1.0-school-search`（基于 `global-study-mvp` 创建）
> 合并：`global-study-mvp`（merge commit `9cae2f0`），`main` 分支未做任何修改
> 线上：https://global-study-mobile-mvp.onrender.com （Render 自动部署，已验证）

## 一、任务完成情况（对照任务书 10 项）

### 1. 学校详情页正式化 ✅
- H5 独立详情页（`#/school/:id` hash 路由）+ 结果卡「查看详情」Bottom Sheet 两种形态。
- 覆盖任务书要求的全部字段：基础资料（名称中英文/类型/国家城市区域）、校区（含地址与坐标）、教育阶段、课程体系、教学语言（主语言 + 附加语言）、学费区间、住宿费与一次性费用、首年总成本估算（附演算说明）、奖学金/助学金列表（条件/比例/截止）、住宿、最近地铁/公共交通（站名 + 线路代码）、到站距离、图片区域、Program 列表（学费/开学/截止/语言要求/收藏）、数据来源（source_url / supplier_evidence）、verified_status、verified_at、有效期、更新时间。
- `is_demo: true` 的学校在页面顶部显示醒目橙色横幅：「⚠️ 演示数据：该学校为演示学校，仅用于验证产品与数据结构，不是真实学校，不代表任何合作关系。」
- 新增测试 `detail: payload covers all 16 spec-required detail sections`，把 16 个必需字段锁进测试，缺一个字段即测试失败（防止字段静默渲染成 "-"）。

### 2. 地图、距离与交通 ✅
- 距离计算：`lib/distance.js` 实现 haversine 大圆距离，返回米数；坐标缺失/非法返回 `null`。
- 全部 142 个站点经纬度已通过 `scripts/migrate-v1.js` 补齐（V0.5 时为 null 占位），20 所演示学校全部可计算到站距离。
- 详情页显示学校经纬度、最近站点（中英文 + 线路代码）、到站距离（米/公里）。
- **通勤时间诚实地显示「暂无可靠通勤数据」**，`commute_label` 在数据层恒为 `null`，不编造任何通勤时间。
- 新增 `GET /api/map/:id`（V1.0 任务 4「地图数据接口」）：返回结构化中心坐标 + 按距离排序的临近站点列表，支持 `maxStations` / `within`（米）参数；`tile_provider` 恒为 `null` —— 未接入地图瓦片组件前，前端显示站点列表与说明文字，不伪造地图。
- 修复 bug：`haversineMeters` 原先把 `null` 坐标按 0 处理，会算出 157 公里的假距离；已修复为返回 `null`。

### 3. 学校对比 ✅
- `GET /api/compare?ids=...` 服务端聚合，超过 4 所自动截断为 4。
- H5 对比页（`#/compare`）：手机端横向滚动表格（`.cmpwrap` overflow-x），高亮项为各校差异项。
- 对比维度覆盖任务书 9 项：国家/城市/区域、教育阶段、课程体系、语言、学费、奖学金、住宿、最近交通、学校类型。
- 上限 4 所，超出时提示「最多同时对比 4 所学校」。

### 4. 收藏与提醒 ✅
- 收藏学校（`gs-favs`）、收藏课程（`gs-fav-progs`），localStorage 持久化，结果页/详情页/B端均可操作，「我的」页有集中视图 + 「我的收藏」筛选。
- 提醒数据结构（`gs-reminders`）：`{ type: 'deadline'|'start'|'scholarship', title, date, schoolId, createdAt }`，覆盖申请截止日期、开学日期、奖学金截止日期三类。
- 站内提醒（「我的」页列表），未接短信/邮件，符合任务边界。

### 5. 真实学校数据导入工具 ✅
- `lib/importer.js`：CSV（RFC4180 兼容解析，含引号转义）与 JSON（数组/对象/`{schools:[]}`包裹）双格式；字段归一化（多别名映射，如 school_name/name/school → name）；字典匹配（国家/城市/区域名或 id 均可，城市解析先限定在已解析国家范围内，避免 "Singapore, Singapore" 误匹配）；字段校验（必填、学费区间、经纬度成对、日期格式、有效期先后、verified 必须带来源与 verified_at）；重复检测（city_id + 归一化名称中英文匹配）。
- API：`POST /api/admin/import/preview`（预览 + 校验 + 重复 + 错误报告，不入库）与 `POST /api/admin/import/commit`（批量导入，仅 admin 角色，`x-role: admin`）。
- 硬性规则：**缺少 source_url 或 supplier_evidence 的数据不得标记 verified**，请求 verified 会报错并强制降级为 draft。
- 三类计数语义：`total = importable + duplicates + invalid`（修复了重复行被重复计数的 bug）。
- 导入的学校带 `id: IMP-xxx`、`is_demo: false`、`source_type: import`，初始 `verified_status: draft`，进入审核队列。
- 详细字段说明见 `REAL_DATA_IMPORT_GUIDE.md`。

### 6. 平台数据审核后台 ✅
- `lib/review.js` 状态机：`draft → pending → verified → expired / rejected`（另支持 request-info 回到 draft、rejected → draft）；非法流转返回 400 拒绝。
- Admin API（均要求 `x-role: admin`）：`GET /api/admin/queue`（含学校详情）、`POST /api/admin/queue/:schoolId/action`（submit / approve / reject / request-info / expire）、`GET /api/admin/schools/:id/revisions`（历史版本与字段级修订记录）、`PATCH /api/admin/schools/:id`（编辑字段）。
- 审核通过（approve）强制要求来源，否则 400「缺少来源的学校不能审核通过」。
- **敏感字段变更强制复审**：学费（tuition_min/max、boarding_fee、one_time_fees、first_year_cost_*）、奖学金、Programs 等字段被修改时，状态自动回退到 pending，修订记录（old → new + 时间）写入历史。
- draft / pending 学校不出现在 C 端搜索；verified 且未过有效期（effective_to）才可见 —— 过期信息不会作为当前事实展示。
- H5 管理后台新增「数据审核」页：审核队列（学校信息 + 历史流转）+ 通过/驳回/要求补充/标记过期按钮 + CSV/JSON 导入工具（预览校验 → 批量导入）。
- 业务数据（queue / imports）持久化在 `data/store/*.json`（git 已忽略运行时写入，仓库内为空种子文件）。

### 7. B端学校资源搜索 ✅
- `GET /api/b2b/schools` 与 `GET /api/b2b/schools/:id`（需 `x-role: agency|admin`，其余角色 403）：学校 + 招生阶段 + 是否接受代理（accepts_agents）+ 合作状态（cooperation_status）+ 佣金字段（类型/数值/币种/结算周期/来源文件/有效期），支持 `acceptsAgents`、`hasCommission` 筛选。
- `GET /api/b2b/programs`（本轮补齐）：B端 Program 视图，含招生阶段、课程、学费、开学/截止、语言要求、所属学校合作状态、协议层级（institution / program 级），支持 q/institution/stage/curriculum/minFee/maxFee/acceptsAgents/hasCommission 筛选。
- **佣金红线**：佣金数据只可能来自 `is_demo: false` 的真实协议记录（`data/store/agreements.json` 中后台录入）；演示协议只显示「该记录为演示数据，无真实佣金信息」，无协议显示「暂无代理协议记录」。不存在任何自动生成、推断或默认佣金的代码路径，并有测试锁定（见测试报告 B2B 部分）。
- H5 B端「学校资源」页新增「学校资源 / Program 列表」双 tab。

### 8. 搜索体验继续优化 ✅（V0.5 基础上增强）
- 国家 → 城市 → 区域级联（切换国家自动清空并重渲染城市/区域选项）。
- 阶段 / Curriculum（按课程族分组）/ Language / Fee 区间 / Scholarship / Boarding / Transit / Institution Type 全部可组合筛选。
- 排序（fee_asc / fee_desc / distance_asc）、分页（meta.total/page/pageSize）。
- 无结果提示、已选筛选条件标签（单个可删 + 全部清除）、一键清除全部筛选。
- 高级筛选 Bottom Sheet 继续由 `/api/reference` 动态生成，无前端写死。
- V1.0 新增：结果卡与筛选支持到站距离（`distance_label`）、`sort=distance_asc`。

### 9. GitHub CI ✅（配置完成，推送受 token 权限限制，见「六、风险与待办」）
- `.github/workflows/ci.yml`：`npm install` → `npm test`（Node 18.x / 20.x 矩阵）→ 启动服务后 curl `/api/health` 校验 `"status":"ok"` + H5 首页包含「学生 / 家长端」角色文案的 smoke test。
- 触发条件：所有分支的 push 与 pull_request，CI 不通过即无法合并。
- **重要说明**：当前 CLI 的 GitHub OAuth token scope 为 `gist, read:org, repo`，GitHub 拒绝推送包含 workflow 文件的提交（需要 `workflow` scope）。CI 文件已写好并在本地分支 `ci/pending-workflow` 保存，且 smoke 步骤已在本地逐条执行验证通过。恢复只需一条命令，见下文「待办」。

### 10. 自动测试 ✅
- V0.5 原 23 个测试全部保留且通过；新增 24 个 V1.0 测试（`tests/v1.test.js`），合计 **47 项**，`npm test` 一条命令跑完两个套件。
- 覆盖任务书要求的全部方向：学校详情（含 16 字段完整性）、compare、favorites、import validation、duplicate detection、review status（状态机 + 敏感字段复审 + 历史）、source validation、distance calculation、B端权限（含 programs 视图、佣金零虚构断言）。
- 测试结束自动清理导入的测试数据，`data/store/*` 恢复空种子状态，不产生脏提交（已用 `.gitattributes` 消除换行符造成的假脏状态）。

## 二、明确没做（遵守边界）
- 未做会员收费 V1.2、完整支付、贷款、住宿交易系统。
- 未引入 Kubernetes / 微服务，仍是单文件 `server.js` + Express + 单文件 H5。
- 未虚构任何学校、排名、佣金、录取概率；`commute_label` 恒为 null；`tile_provider` 恒为 null。
- 未删除/破坏 C/B/S/Admin 四端入口与既有业务流（bootstrap、applications、clients、audits、reset 均保留并有测试）。
- 未修改 `main` 分支。
- 未导入任何真实学校数据（工具已就绪，等待运营提供带来源的数据）。

## 三、本轮修复的真实 bug
1. `lib/distance.js`：`Number(null) === 0` 导致缺失坐标按 (0,0) 计算，返回 157km 假距离 → 现返回 `null`。
2. `lib/importer.js` `parseInput`：不可解析文本返回 `[]`（假装解析成功）→ 现返回 `null`，预览接口报 400。
3. `lib/importer.js`：重复行同时计入 `invalid` 与 `duplicates`，计数不闭合 → 重复单独计数。
4. `lib/importer.js`：城市解析未限定国家范围，「Singapore, Singapore」会命中国家字典得到错误 `city_id` → 先在国家内解析。
5. `lib/importer.js`：导入学校只有 `name` 字段而 C 端搜索读 `name_zh/name_en`，导致审核通过后搜索查不到 → 按语言自动补齐中英文名。
6. `public/index.html`：地图区域线路代码被写成未插值的字面量 `${esc(s.line.code)}` → 已修复（浏览器验收时发现）。
7. `server.js`：审核动作 note 文案错误（submit 显示「标记过期」）→ 已修正为「提交审核」。

## 四、UI / 兼容性验收（浏览器自动化，真实渲染）
- 视口 375 / 390 / 430 三档：横向溢出均为 0px；结果卡宽度 332/347/387px；卡片操作按钮与底部导航等宽不重叠。
- 完整链路走查：首页 CTA → 找学校（20 所 · 第 1 页，级联下拉/快捷 chips/高级筛选入口/筛选标签/一键清除）→ 结果卡详情 Bottom Sheet（演示横幅 + 到站距离 712 米）→ 独立详情页（最近 5 站点列表 + 暂无可靠通勤数据 + 首年成本 + 数据来源）→ 对比页（3 校、24 个差异高亮格、横向滚动可用）→ B端学校资源 / Program 列表双 tab → 管理后台审核队列 + 导入工具。
- 四端回归：student / agency / supplier / admin 四套导航与页面渲染全部正常，localStorage 角色记忆保留。
- 线上验收：部署完成后在 390px 视口实测线上 H5，搜索、详情、地图区域、演示标识全部正常。

## 五、部署状态
- `global-study-mvp` 已推送（`c86de0f`），Render 自动部署完成。
- 线上验证（2026-09-23）：`/api/health` 返回 V1.0 字段（b2bEnabled、reviewQueue、importedSchools）；`/api/map/:id`、`/api/compare`、`/api/b2b/programs`（无角色 403 / agency 200）、`/api/admin/queue`（无角色 403）全部符合预期；线上 H5 详情页地图区域、演示横幅、通勤诚实占位均正常。

## 六、风险与待办
1. **CI 文件尚未推到远端**（唯一未闭环项）：当前 token 缺 `workflow` scope。恢复步骤：
   ```
   gh auth refresh -h github.com -s workflow   # 浏览器授权一次
   git checkout ci/pending-workflow -- .github/workflows/ci.yml
   git add .github && git commit -m "ci: restore workflow file" && git push
   ```
   在此之前 GitHub 上没有 CI 门禁，合并靠本地 `npm test`（47 项全绿）保障。
2. 站点/学校坐标部分为种子数据，导入真实学校时需同时提供可靠坐标，否则距离字段诚实返回 null。
3. `data/store/agreements.json` 目前只有 2 条演示协议；第一条真实协议录入前，B端佣金字段将全部显示「未录入」，这是设计使然。

## 七、结论
V1.0 十项任务全部完成并验收，真实数据导入的机制、校验、审核、溯源链路已就绪并有测试保障。**已达到可以进入真实学校数据导入阶段的条件**（详见最终汇报）。
